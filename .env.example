# 数据库配置
DB_USERNAME=root
DB_PASSWORD=your_secure_password_here

# JWT 密钥（生产环境请修改为随机字符串，至少32位）
JWT_SECRET=change-this-to-a-random-string-at-least-32-characters-long

# Spring Profile（可选，默认为 prod）
SPRING_PROFILES_ACTIVE=prod
